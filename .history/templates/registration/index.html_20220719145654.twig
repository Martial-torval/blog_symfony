{% extends 'base.html.twig' %}

{% block title %}Hello LoginController!{% endblock %}

{% block body %}
{{ form_start(registration_form, {'attr':{'class':'w-50 mx-auto'}})}}
{{ form_label(registration_form.username)}}

{{ form_widget(registration_form.username)}}
{{ form_label(registration_form.password)}}
{{ form_widget(registration_form.password)}}

{{ form_label(registration_form.submit)}}
{{ form_widget(registration_form.password)}}
<a href="{{path('login')}}"><span> U have an account ? Login here ! </span></a>
{% endblock %}
