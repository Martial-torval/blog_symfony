{% extends 'base.html.twig' %}
{% block body %}
<section class="">
<h3>{{ post.title }}</h3>
<p>{{ post.content }}</p>
<span>{{ post.category.nameCategory }}</span>
<img src="{{ post.illustration }}" alt="img-post">
</section>

{% endblock %}

