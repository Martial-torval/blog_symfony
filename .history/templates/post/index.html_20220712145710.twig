{% extends 'base.html.twig' %}
{% form_theme post_form 'bootstrap_5_layout.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
{{ form_start(post_form) }}

{{ form_errors(post_form) }}

{{ form_label(post_form.title)}}
{{ form_widget(post_form.title, {
    'attr' => {
        'background-color' => '#101820',
        'color' => 'wheat'
    }})
}}
{# {{ form_widget(form.field, { 'attr': {'class': 'bar'} }) }} #}


{{ form_label(post_form.content)}}
{{ form_widget(post_form.content)}}

{{ form_label(post_form.category)}}
{{ form_widget(post_form.category)}}
{{ form_end(post_form) }}
{% endblock %}
